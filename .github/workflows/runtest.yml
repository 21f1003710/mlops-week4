name: Sanity test

on:
  push:
    branches: [master, dev]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  train-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --prefer-binary --no-cache-dir pandas numpy scikit-learn tensorflow matplotlib

      - name: 🏋️‍♀️ Train model and save metrics
        run: python train.py

      - name: 🧪 Run tests
        run: python test.py > test_output.txt

      - name: 🛠 Set up CML
        uses: iterative/setup-cml@v2

      - name: 📝 Create and post CML report
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "## 🧪 Test Results" > report.md
          echo '```' >> report.md
          cat test_output.txt >> report.md
          echo '```' >> report.md

          echo -e "\n## 📈 Metrics from Training\n" >> report.md
          echo '```csv' >> report.md
          tail -n 5 models/metrics.csv >> report.md
          echo '```' >> report.md

          cml comment create report.md
